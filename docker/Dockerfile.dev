FROM node:24-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

# Create empty .env file if it doesn't exist (env vars are injected by docker-compose)
RUN touch .env || true

EXPOSE 3000

CMD ["npm", "run", "dev"]
